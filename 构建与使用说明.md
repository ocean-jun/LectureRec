# 课堂转写 LectureRec — 构建与使用说明

为课堂场景做的 Android 云端 AI 语音转录 App。常驻后台、息屏继续录、实时滚动转写、一键复制、逐句落盘。

---

## 1. 产物

| 文件 | 大小 | 说明 |
|---|---|---|
| `dist/app-release.apk` | 24.9 MB | **推荐安装**，`isDebuggable=false` |
| `dist/app-debug.apk` | 26.1 MB | 调试版，含调试工具 |

- 包名 `com.dsh.lecturerec`，**版本 2.0 (versionCode 8)**，应用名 **LectureRec**
- minSdk 26（Android 8.0+），targetSdk 34
- **怎么确认手机上是哪一版**：设置页顶部会显示「版本 2.0 (8) · 安装于 …」，
  一眼就能对上是不是最新构建。（早期版本号一直没升过，手机上永远显示同一个数字，分不清。）
- **只含 arm64-v8a**。这 24 MB 里约 20 MB 是 ONNX Runtime 的原生库（Silero VAD 要用），
  只保留 arm64 是为了不让 APK 再翻一倍。要支持 32 位老机型，在 `app/build.gradle.kts`
  的 `ndk { abiFilters }` 里加回 `armeabi-v7a`。
- 内置 `assets/silero_vad.onnx`（2.2 MB，取自 silero-vad 官方仓库）
- 签名：APK Signature Scheme **v2**，debug 密钥（`CN=Android Debug`）
  → 侧载可直接安装；将来如果用正式密钥签名，需先卸载再装

### 权限（只有 6 条，无位置/通讯录/存储/广告）

```
RECORD_AUDIO, INTERNET, FOREGROUND_SERVICE,
FOREGROUND_SERVICE_MICROPHONE, POST_NOTIFICATIONS, WAKE_LOCK
```

另外两项安全设置：
- `allowBackup=false` —— 禁止系统云备份把 API Key 带走
- `usesCleartextTraffic=false` —— 禁止明文 HTTP，Key 和音频不会被明文外发

---

## 2. 安装

手机开启「USB 调试」后（adb 在本机 `C:\adb\platform-tools\adb.exe`）：

```powershell
C:\adb\platform-tools\adb.exe devices
C:\adb\platform-tools\adb.exe install -r D:\dsh\LectureRec\dist\app-release.apk
```

或把 APK 传到手机 → 允许「安装未知应用」→ 点击安装。

---

## 3. 首次配置（关键，只有一次）

打开 App → 右上角菜单 → **设置**：

| 字段 | 填什么 |
|---|---|
| 服务预设 | 选 **硅基流动 · TeleSpeechASR（免费 · 快）** ← 默认就是这个 |
| 接口地址 | 自动填入 `https://api.siliconflow.cn/v1` |
| API Key | 填你自己的 Key |
| 模型名 | 自动填入 `TeleAI/TeleSpeechASR` |
| 语言代码 | `zh` |
| 课程热词 | 见下方 |
| 课程名 | 例如「高等数学」，用于生成文件名 |
| AI 校对 | 要校对的话打开开关，填 DeepSeek Key |
| 断句引擎 | 保持「自动」即可 |

### 两个免费模型怎么选（实测数据）

同一部手机、同一网络、同一晚实测：

| 模型 | 单次请求延迟 | 标点 |
|---|---|---|
| **`TeleAI/TeleSpeechASR`**（默认） | **0.4 ~ 2.0 秒**（26 次请求） | 句末标点较少 |
| `FunAudioLLM/SenseVoiceSmall` | 平均约 **30 秒**，最慢 98 秒 | 标点完整、断句更像书面语 |

两个都免费。SenseVoice 的免费档当时**严重排队**（甚至出现过 120 秒内一次都没返回），
所以默认选了快 30 倍以上的 TeleSpeech。

如果你更看重标点和书面感、且能接受等待，在设置里换成 SenseVoice 即可（也是免费）。
**建议先用两段真实课堂录音各试一次再定。**

也可以选 **阿里云百炼 · qwen3-asr-flash**（约 ¥0.79/小时，中文识别的第一梯队）。
它的语音识别**不走** `/audio/transcriptions`，但 App 会按地址自动切换协议（见第 7 节），
你不需要额外配置任何东西。

> **注意**：接口地址必须是 `api.siliconflow.cn`，**不是** `api.siliconflow.com`。
> 后者会把正确的 Key 报成 `401 Token is invalid`，这是实测踩过的坑。

### 课程热词怎么写

逗号或换行分隔，例如：

```
拉格朗日乘子, 酉矩阵, 傅里叶变换, 特征值, 正交基, 薛定谔方程
```

热词会通过转写接口的 `prompt` 参数送给模型，**专有名词的同音错字能砍掉一大半**。
这一项的性价比远高于换更贵的 ASR 模型。

---

## 4. 使用

- **开始听课**：点一下即开始。通知栏出现常驻通知（显示时长/句数/待上传数）
- **息屏/切后台**：照常录音转写，不需要保持屏幕亮
- **复制全文**：一键把全部文本（含时间戳之外的正文字）复制到剪贴板
- **长按某一句**：只复制那一句
- **AI 校对**：整篇送给 LLM 修同音字、补标点、分段。结果弹窗里可再复制或另存
- **菜单 → 导出/分享**：把 Markdown 文件分享到微信/网盘等
- **菜单 → 重传失败片段**：网络抖动导致失败的片段会留底，重连后一键补传
- **菜单 → 清空文本**：只清界面，已保存的文件不受影响

文件位置（无需任何存储权限）：

```
/sdcard/Android/data/com.dsh.lecturerec/files/transcripts/2026-09-21_1430 高等数学.md
```

**每句话一落盘**，所以进程被杀也不会丢内容。

---

## 5. 让系统别杀它（各厂商路径不同，必须设）

前台服务 + WakeLock 已经能覆盖绝大多数情况，但国产 ROM 还需要手动放行：

| 品牌 | 路径 |
|---|---|
| 小米/Redmi | 设置 → 应用设置 → 课堂转写 → **省电策略 → 无限制**；**自启动 → 允许** |
| 华为/荣耀 | 设置 → 应用 → 应用启动管理 → 课堂转写 → 关掉「自动管理」，全部手动勾选 |
| OPPO/一加/realme | 设置 → 电池 → 应用耗电管理 → 课堂转写 → 允许**后台运行** + 允许**自启动** |
| vivo/iQOO | 设置 → 电池 → 后台高耗电 → 允许；i管家 → 自启动管理 → 允许 |
| 原生 Android | 设置 → 应用 → 课堂转写 → 电池 → **不受限制** |

> 不做这一步，「常驻后台」会时灵时不灵 —— 这是此类 App 最常见的失败原因。

---

## 6. 费用（按每周 5 天 × 2 小时 = 40 小时/月算）

| 项目 | 单价 | 40h/月 | 一学期(720h) |
|---|---|---|---|
| 硅基流动 SenseVoiceSmall | **当前免费** | ¥0 | ¥0 |
| 硅基流动 TeleSpeechASR | **当前免费** | ¥0 | ¥0 |
| Groq whisper-large-v3-turbo | $0.04/小时 | ≈¥11 | ≈¥207 |
| 阿里云 qwen3-asr-flash | ¥0.79/小时 | ≈¥32 | ≈¥570 |
| DeepSeek `deepseek-flash` 校对 | 约 ¥0.3/节课 | ≈¥6 | ≈¥108 |

**「免费」是价格页当前标注，不是承诺。** 厂商改价时你只需在设置页改模型名，不用重新编译。

省钱机制：VAD 会掐掉静音段不发送。课堂上有效语音通常只占 60~70%，等于直接省三成。

---

## 7. 架构

```
AudioRecord(16kHz / 单声道 / PCM16，320 样本 = 20ms 一帧)
  └─ VadSegmenter        迟滞双阈值 + 预卷 + 最短/最长时长（状态机）
       └─ SpeechDetector
            ① SileroDetector → Silero VAD（ONNX Runtime）
            ② EnergyDetector → RMS vs 自适应噪声底（兜底）
            由 FallbackDetector 包住：Silero 连续失败 10 次自动降级
       └─ 每句裸 PCM → Wav.wrap() 加 WAV 头
            └─ Channel 单消费者队列（保证文本顺序 = 录音顺序）
                 └─ AsrEngine
                      ① AsrClient          POST {base}/audio/transcriptions（multipart）
                      ② DashScopeAsrClient POST {base}/chat/completions（base64 内联）
                      └─ TranscriptRepo(内存) + TranscriptStore(逐句落盘 .md)
```

- **前台服务** `RecorderService`：`startForeground(..., FOREGROUND_SERVICE_TYPE_MICROPHONE)` + `PARTIAL_WAKE_LOCK`
- **热词**：注入 ASR 的 `prompt`（仅 OpenAI 兼容路径）和 LLM 校对提示词

### VAD 引擎（设置页可切：自动 / 只用 Silero / 只用能量法）

- Silero 在 16 kHz 下要 **576** 个样本 =（上一块末尾 64 样本上下文 + 本块 512 样本），
  与 320 样本的采集帧不是整数倍，由 `SileroWindower` 负责重切。
- 模型签名**运行时自适应**：v5 用 `state`[2,1,128]，v4 用 `h`/`c`[2,1,64]。
  实测该模型对音频输入的 shape 声明是 `[-1,-1]`（全动态），所以窗口长度只能按约定来，读 shape 得不到信息。
- 加载失败、或推理连续失败 10 次，都会**自动降级到能量法** —— 绝不因为模型问题让整堂课录不下来。
- 为什么要 Silero：能量法只能区分「响」和「不响」，翻书、挪椅子、空调气流都会被当成语音，
  而这些片段发给 ASR 常常换来一段幻觉文本。教室里这类噪声非常多。

### 收音灵敏度与音源（设置页可切：高 / 中 / 低）

用户反馈「**要很大声才触发录音**」，查下来是两个原因叠加：

**① 音源选错了（主因）。** 原本用 `AudioSource.MIC` —— **原始信号，没有自动增益**。
手机放课桌上、老师在三五米外，数字电平只有满量程的百分之几，VAD 拿这种电平自然给不出高概率。
现在改用 **`VOICE_RECOGNITION`**（系统为语音识别调过的音源，**带 AGC**），
个别机型不提供该音源会自动退回 `MIC`。

**② 起判阈值偏高。** 现在默认灵敏度「高」，把 Silero 的起判阈值从 **0.50 降到 0.30**。

**为什么降阈值不会牺牲准确率** —— 有实测数据支撑：

```
电平峰值 rms=1940   人声概率峰值=0.61   ← 正常说话，能触发
电平峰值 rms=4108   人声概率峰值=0.06   ← 更响，但概率只有 0.06（椅子/敲击之类）
```

**响度更高的那个反而被判为「不是人声」** —— 说明 Silero 靠的不是音量而是语音特征。
所以阈值降到 0.30 **不会**放进这类噪声，只会接住原本漏在 0.3~0.5 之间的真人声。

如果教室很嘈杂、开始出现莫名其妙的片段，到设置里把灵敏度调回「中」或「低」即可。

**诊断用**：每次录音的前 30 秒，每 2 秒会往 logcat 打一条电平（`adb logcat -s LectureRec:D`
可以看到 `电平峰值 rms=… 人声概率峰值=…`）。这是判断「该调灵敏度还是该换音源」的唯一依据，
不想要就等 30 秒，不会一直刷。

### ASR 协议（按 Base URL 自动判别，见 `AsrEngineFactory`）

| 地址特征 | 协议 | 厂商 |
|---|---|---|
| 含 `dashscope` 或 `maas.aliyuncs.com` | `/chat/completions`，音频以 base64 data URL 内联 | 阿里云百炼 |
| 其余 | `/audio/transcriptions`，multipart 上传文件 | 硅基流动 / Groq / OpenAI / 自建 |

> 百炼的「OpenAI 兼容模式」并不提供 `/audio/transcriptions`，它复用 `/chat/completions`。
> 百炼的热词走 system message，但官方文档给出的字段结构不完整，贸然发送可能让每个请求都 400，
> 所以**那条路径暂不发热词**（OpenAI 兼容路径正常支持）。
> 如果你自建了 DashScope 协议兼容的代理，域名里请带上 `dashscope` 或 `maas.aliyuncs.com`。

### 上传策略：攒批 + 并发

> **注意**：这一节的参数是给「每请求 30 秒」的慢模型调的。
> 换成 TeleSpeechASR（每请求 0.8 秒）之后已经整体压小，见本节末尾。

最早的问题是**每个请求的固定开销**（当时约 30 秒，与音频长短基本无关）。
一个 2 秒的片段也要等 30 秒 —— 等于 15 倍浪费。所以策略围绕「让每次请求搬运更多音频」：

| 机制 | 说明 |
|---|---|
| **攒批** | 队列里时间连续的若干句拼成一批再发一次请求 |
| **停顿即发** | 说话人停顿一段时间就收工发车，不必硬凑满目标时长 |
| **延迟上界** | 从第一句起最久等这么久，一定要发出去 |
| **并发上传** | 默认 5 路（设置页可调 1/2/3/5/8） |
| **失败快速判定** | 401/404 这类配置错误不再退避重试，省掉每段 3.6 秒的空等 |

这套参数在慢模型下把吞吐从 **0.063x 提到 0.211x**（3.3 倍），
并且**消除了"录音越久积压越多、永远追不回来"的问题**。

**换成快模型后重新压小**（v1.7）：

| 参数 | 慢模型时代 | v1.7 | 理由 |
|---|---|---|---|
| 每批目标 | 15 秒 | **8 秒** | 攒批收益消失，只剩延迟 |
| 停顿即发 | 3000ms | **800ms** | 首次出字的头号大头 |
| 延迟上界 | 20000ms | **10000ms** | 同上 |
| 断句静音阈值 | 600ms | **600ms（不动）** | 降了会从句子中间切断，伤准确率 |

**实测效果**：首次出字 **约 4.5 秒 → 约 2.3 秒**（请求本身 0.94s + 断句 0.6s + 攒批 0.8s）。

另外启动顺序也改了：**先开麦克风、再加载 Silero 模型**。
模型加载要 235ms，放在前面的话「按下按钮就说话」会直接丢掉第一个字。
现在这段时间的音频先进 AudioRecord 的内部缓冲（≥1 秒），实测 `AudioRecord started (+19ms)`（原先 +247ms）。

> OkHttp 默认每个主机只允许 5 个并发请求，是个隐形天花板，已在 `Http.kt` 调到 16，
> 否则设置里选 8 路也跑不满。

### 时间戳显示（设置页可调）

| 模式 | 效果 |
|---|---|
| **每分钟标一次**（默认） | 只在跨分钟时插一个 `[00:01]`，中间的文字连成一片 |
| 每段都标 | 每段前置 `[00:01:23]` |
| 不标时间 | 完全不带 |

**「复制全文」本来就不带时间戳**，贴到别处始终是干净的纯文本。

### 文件写入顺序

并发上传后片段**完成的先后顺序是乱的**，但文件必须按**录音顺序**写，
否则导出的笔记时间戳会来回跳。`TranscriptWriter` 负责：
只写连续的、已进入最终状态的片段；中间卡着一个还在上传的就停下来等；
失败的片段跳过但不阻塞后面的（音频已留底，可手动重传）。

---

## 8. 重新编译

```powershell
$env:JAVA_HOME='D:\dsh\build-env\jdk'
$env:ANDROID_HOME='D:\dsh\build-env\android-sdk'
$env:GRADLE_USER_HOME='D:\dsh\build-env\gradle-home'

& 'D:\dsh\build-env\gradle\bin\gradle.bat' -p 'D:\dsh\LectureRec' --no-daemon assembleDebug
& 'D:\dsh\build-env\gradle\bin\gradle.bat' -p 'D:\dsh\LectureRec' --no-daemon assembleRelease
& 'D:\dsh\build-env\gradle\bin\gradle.bat' -p 'D:\dsh\LectureRec' --no-daemon testDebugUnitTest
```

工具链：JDK 17.0.20.1 / Gradle 8.0 / AGP 8.1.2 / Kotlin 1.8.10 / Android SDK 34。

**沙箱注意**：本会话的沙箱在 `workspace-write` 模式下一律拒绝执行外部程序，
所以上面命令需要提权（`danger-full-access`）才能跑。
提权后 DNS 与 HTTPS 直连正常，**不需要** `build-env/proxy.mjs`。
（`gradle-home/gradle.properties` 里的代理配置已备份为 `gradle.properties.proxy-backup`）

---

## 9. 验证记录

### 单元测试：105 个全部通过（17 个测试类）

```
AsrClientTest          11   multipart 线格式、URL 拼接、SenseVoice 标记清除、
                            可选字段省略、Key 缺失快速失败、HTTP 错误透出、
                            畸形 JSON 必须报错而不是污染正文
DashScopeAsrClientTest  8   /chat/completions 线格式、base64 音频可解码还原、
                            控制标记清除、缺 choices 必须报错
AsrEngineFactoryTest    5   协议判别（百炼 vs OpenAI）、预设与协议对得上
PolishClientTest        3   /chat/completions 线格式、热词进提示词、HTTP 错误
BatchFormerTest        10   攒到目标时长、句数封顶、停顿即发、延迟上界、
                            间隔小于空闲阈值就继续攒、不丢句、剩下的留队列
TimestampsTest          7   三种模式、跨分钟判定、跨小时、标签格式、枚举往返
TranscriptWriterTest    7   只写连续已完成段、遇到未完成停下、失败段跳过不阻塞、
                            乱序完成仍按录音顺序写（回归测试）
RetryPolicyTest         6   配置错误不重试、401/404 不重试、429/5xx 重试、
                            传输错误重试、对着客户端真实文案测
PcmTest                 6   拼接顺序与内容、单段不拷贝、超大块、
                            时长换算、不改动入参
VadSegmenterTest        9   纯静音不产段、单次语音产一段、低于进入阈值不起录、
                            迟滞跨过断句窗仍不断句、短促声丢弃、连续语音强制切分、
                            finish() 收尾、检测器失效时按静音处理、语音占比
SileroWindowerTest      6   窗口长度符合 Silero 约定、上下文取上一块末尾、
                            一帧过长时产出多个窗口且不丢样本（回归测试）
EnergyDetectorTest      6   静音/响亮判定、阈值钉住重构前的手调值、已知局限
FallbackDetectorTest    4   连续失败后降级、降级后阈值跟着换、零星失败不降级
RetryStoreTest          3   失败片段文件名跨会话唯一、按时间排序（回归测试）
TranscriptStoreTest     8   两种时间戳格式、无时间戳行是正文不是脏数据、
                            跨小时、往返还原、只丢标题与空行
WavTest                 2   WAV 头逐字段、PCM 载荷原样拷贝
TranscriptRepoTest      4   顺序、校对稿优先、空段跳过、定点更新
```

### 真机验证：Pixel 3 / Android 12 (SDK 31) / arm64-v8a

实测日志（`adb logcat -s LectureRec:D`）：

```
I LectureRec: startRecording model=FunAudioLLM/SenseVoiceSmall keySet=false
I LectureRec: session file = .../transcripts/2026-09-21_2049 课堂记录.md
I LectureRec: AudioRecord started: 16000Hz mono pcm16
D LectureRec: segment #1 start=5340ms dur=2220ms bytes=71040
W LectureRec: segment #1 attempt 1/2/3 failed: 未配置 API Key
E LectureRec: segment #1 FAILED after 3 attempts
I LectureRec: capture ended: 29s 音频, 有效语音占比 25%
I LectureRec: teardown error=... done=0 failed=5
```

| 能力 | 结果 |
|---|---|
| 安装 / 启动 | ✅ 无崩溃，`mCurrentFocus` 为 MainActivity |
| 前台服务 | ✅ `isForeground=true foregroundId=1001`，通知带「停止」按钮 |
| **息屏继续录** | ✅ 按电源键熄屏 8 秒后 `isForeground` 仍为 true |
| 麦克风采集 | ✅ `AudioRecord started: 16000Hz mono pcm16` |
| VAD 切句 | ✅ 30 秒内切出 5 段：940 / 2120 / 1420 / 1500 / 1600 ms |
| **掐静音** | ✅ 实测有效语音占比仅 **25%**（安静时 1%），静音确实没上传 |
| WAV 编码 | ✅ 落盘文件 30124 字节 = 44 头 + 30080 PCM，与日志 `bytes=30080` 吻合 |
| 失败重试 | ✅ 3 次递增退避，失败后音频留底 |
| 留底不覆盖 | ✅ 5 段生成 5 个不同文件名；跨会话计数 5 → 6 正确累加 |
| 连录两次 | ✅ 同一 App 内可反复开始/停止，无需重启 |
| 界面状态 | ✅ 「时长 00:00:30　已转写 0 句 · 失败 6」+「未配置 API Key」错误上屏 |
| 落盘 | ✅ 每个会话生成独立的 `.md` 文件 |

### 真机测试中发现并修掉的 3 个真实缺陷

1. **每秒无条件重绘状态栏**：未录音时也有个 1 秒定时器改写 TextView，每秒触发一次布局 ——
   白耗电，且导致界面永远进不了 idle 状态（`uiautomator dump` 持续报 `could not get idle state`）。
   改为只在录音时刷新、且文本未变化就不写回。
2. **麦克风被抢占就静默停录**：`readFrame` 一失败就跳出循环，后半堂课全丢且用户不知情。
   改为最多 5 次重开麦克风；彻底失败时除状态栏外**额外发一条「录音已停止」通知**。
3. **失败片段跨会话互相覆盖**：段 id 每会话从 1 开始，文件名撞车会把上一堂课留底的音频覆盖掉（静默数据丢失）。
   改为 `会话时间戳_段id.wav`，并加了回归测试。

以上三个都是单元测试抓不到的，只有真机能暴露。

### 第三轮新增：Silero VAD 与双协议（同一台 Pixel 3 实测）

```
I LectureRec: ASR 协议: OpenAI 兼容 /audio/transcriptions model=FunAudioLLM/SenseVoiceSmall
I LectureRec: silero 模型已载入，输入张量: [input, state, sr]
I LectureRec: silero 音频输入形状: [-1, -1]
I LectureRec: silero 变体: v5
I LectureRec: VAD 引擎: silero-v5
I LectureRec: AudioRecord started: 16000Hz mono pcm16
D LectureRec: segment #1 start=880ms dur=1380ms bytes=44160
D LectureRec: segment #2 start=2920ms dur=10060ms bytes=321920
```

| 项 | 结果 |
|---|---|
| Silero 模型加载 | ✅ 从 assets 载入，输入张量识别为 v5 |
| 变体自适应 | ✅ 音频输入 shape 实测为 `[-1,-1]`（完全动态）——印证窗口长度只能按约定来，读 shape 得不到信息 |
| Silero 推理 | ✅ 正常出段，无 native 错误、无崩溃 |
| 切句行为变化 | ✅ 25 秒出 2 段（能量法 30 秒出 5 段），明显更保守，少把噪声当人声 |
| ASR 协议判别 | ✅ 硅基流动地址正确判为 `/audio/transcriptions` |

### 第三轮修掉的缺陷

4. **`SileroWindower` 会静默丢样本**：凑出第一个窗口就 `return`，如果某一帧比 shift 还长，
   该帧剩余样本被悄悄丢掉。生产参数（320 < 512）撞不到，但一旦改采样率就会踩中。
   改为返回窗口列表，并加了「一帧 10 样本产出 2 个窗口、余 2 个留在缓冲」的回归测试。
5. **降级分支从来没被测到过**：`FallbackDetector` 里的 `Log.w` 在单元测试中直接抛
   `android.util.Log not mocked`（和 `org.json` 同一类问题）。打开
   `testOptions.unitTests.isReturnDefaultValues` 之后这条路径才真正被覆盖。

### 第五轮：上传策略与时间戳（发现并修掉 3 个缺陷）

**性能**：把实测数据摊开后发现，瓶颈是服务端**每请求的固定开销**（约 30 秒，与音频长短无关）：

| 批次 | 音频 | 耗时 |
|---|---|---|
| #6 | 9360ms | **0.85 秒** |
| #3 | 560ms | **37.0 秒** |

同一部手机、同一个网络：9.4 秒的音频 0.85 秒就回来，0.56 秒的音频却要 37 秒。
服务端有能力秒回，大部分请求是被压在队列里 —— **这是免费档限流，App 侧碰不到**。
能做的只有把固定开销摊薄：攒批 + 并发，吞吐 0.063x → 0.211x，积压问题消失。

**修掉的 3 个缺陷**：

9. **并发导致文件顺序错乱**（本轮引入的回归）：并发后片段完成顺序是乱的，
   按完成顺序追加会让导出的笔记时间戳**来回跳**（实测出现 `[00:00:17]` 后跟 `[00:00:16]`、
   再跟 `[00:00:01]`）。新增 `TranscriptWriter` 按录音顺序写入，并加了 7 个测试。
10. **新会话与恢复出来的历史编号撞车**：启动时会从上次的文件把内容恢复进内存（编号 1..N），
    而新会话的批次**又从 1 开始编号**。按序写入的逻辑于是把**上一堂课的内容当成"该补写的历史片段"**
    写进了新文件 —— 日志里出现过「按序写入 63 段」，而那一轮其实只录了 3 批。
    改为开始录音前清空内存里的历史。
11. **改时间戳模式会让旧文件解析失败**：改成「每分钟一次」后大部分行没有时间戳，
    而原来的解析器只认 `[HH:MM:SS]` 开头的行 —— 重新打开 App 会把没时间戳的行**全部丢掉**。
    解析器改为同时接受 `[HH:MM:SS]`、`[HH:MM]` 和纯正文行（只有空行与 `#` 标题才丢弃）。

### 第六轮：找到并解决延迟的真正原因

上一轮我判断「瓶颈是免费档限流，App 侧碰不到」——**这个判断只对了一半，而且我找错了方向。**

真相是：**同一个平台的两个免费模型，速度差了 30 倍以上。**

| 模型 | 请求数 | 平均延迟 | 最快 | 最慢 |
|---|---|---|---|---|
| `FunAudioLLM/SenseVoiceSmall` | 多次 | **约 30 秒** | 0.85s | 98s |
| **`TeleAI/TeleSpeechASR`** | **26 次** | **0.8 秒** | **0.4s** | 2.0s |

26 次请求**全部**在 2 秒内返回。换模型后真机实测（v1.6 release）：

```
I LectureRec: ASR 协议: OpenAI 兼容 /audio/transcriptions model=TeleAI/TeleSpeechASR
10 次请求：平均 0.8s，最快 0.4s，最慢 2.0s
```

**端到端延迟从 30~40 秒降到几秒。**

不是网络（ping 0% 丢包、61ms RTT）、不是并发数（串行时也是 30 秒）、
不是我的代码 —— 是 **SenseVoiceSmall 那个模型在免费档上排队**。

**代价**：TeleSpeech 的句末标点明显比 SenseVoice 少，个别词也会偏
（实测把「长难句」识别成「苍南句」）。所以这是**速度 vs 标点/书面感**的取舍，
两个都免费，设置页可随时切换，建议用真实课堂录音各试一次再定。

**方法上的教训**：我上一轮用「同一模型不同批次耗时差异大」推断出"服务端限流"，
却没有想到**换一个模型试试**——明明是同一个平台上另一个免费选项，一行配置就能验证。

### 仍未验证

- ~~真实转写成功路径~~ → **已验证**：你配好 Key 后已产生多份真实转写文件（含标点、口语内容正确）。
- **连续 2 小时录音**的内存与稳定性（目前最长只验证过约 15 分钟）。
- **国产 ROM 的后台限制**：当前验证机是 Pixel 3 原生 Android 12，没有小米/华为那类激进省电策略，参考价值有限。
- **两个模型的真实课堂准确率对比**：只在非正式场景试过，没有用同一段课堂录音做过 A/B。

**报告问题**：把 `adb logcat -s LectureRec:D` 的输出发我。

